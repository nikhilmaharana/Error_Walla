# Day-21 – Advanced Routing Patterns

## Concepts Covered
- Nested routes
- Code-splitting
- Lazy loading with React.lazy
- Suspense fallback UI

## How It Works
- Routes are nested using Layout + Outlet
- Pages are lazy-loaded to reduce initial bundle size
- React Suspense shows loading fallback

## How to Run
```bash
npm install
npm run dev
